const writeNumbers = (numbers: number[]): void => {
  for (const number of numbers) {
    console.log(number);
  }
};

const numbers: number[] = [1, 2, 3, 4, 5];
writeNumbers(numbers);
// Output: 1, 2, 3, 4, 5
